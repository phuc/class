<!DOCTYPE html>
<html>
<head>  
  <%= render "layouts/head_meta" %>
  <%= javascript_include_tag "//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js", "application" %>
  <style type="text/css">
  html {overflow: auto;}
  html, body, div, iframe {margin: 0px; padding: 0px; height: 100%; border: none;}
  iframe {display: block; width: 100%; border: none; overflow-y: auto; overflow-x: hidden;}
  #logged_in {
    position: fixed;
    right: 20px;
    top: 0px;
    color: #FFFFFF;
    text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.25);
    font-family: 'Ubuntu',Tahoma,sans-serif;
    font-size: 14px;
    display: none;
  }
  #logged_in a {
    color: #DD4814;
    text-decoration: none;
  }
  #logged_in a:hover,
  #logged_in a:focus {
    text-decoration: underline;
    color: #DD4814;
  }
  </style>
</head>
<body>
  <% if logged_in? %>
  <div id="logged_in">
    <p>
      Welcome <%= @username %>!&nbsp;|&nbsp;
      <%= link_to "Sign out", signout_path, :method=>:delete, :id=>"signout_link" %>
    </p>
  </div>
  <% end %>
  <%= yield %>  
</body>
</html>